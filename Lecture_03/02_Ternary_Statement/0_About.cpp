/*
TERNARY STATEMENT :- 1. Ternary means --> 3
                     2. convert cout into a single-line'.  
NOW,
    Convert Code to Ternary Statement :-

    Code:-
        if(condition) {
    
        }
        else {
    
        }

    Ternary Statement:-
    
        cout<<(condition?stt1:strr2)<<endl;     ---> syntax of Ternary statement.
        
        Here,
            1. ':' ---> Separate both conditions.
            2. when conditon? is 'true' ---> stt1
            3. when condition? is 'false' ---> strr2
*/


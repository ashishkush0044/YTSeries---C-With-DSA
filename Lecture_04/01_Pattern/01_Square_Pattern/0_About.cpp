/*
SQUARE PATTERN :- In C++, a square pattern usually refers to printing a square
                  shape made of characters (like * or #) using nested loops.
                  
NOTE :- IF i=0, j=0 then we can write loop.
here,
    for(int i=0; i<n; i++)  
            or    
    for(int i=0; i<=n-1; i++)
*/

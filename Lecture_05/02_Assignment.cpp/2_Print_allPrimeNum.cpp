/*
2. WAF to print all Prime Numbers from 1 to N.
*/

#include <iostream>
using namespace std;

int allprime(int num) {
    int count=0;
    for(int i=2; i<=num; i++) {
        if(num%2==0) {
            count++;
        }
    if(num)
    }
}
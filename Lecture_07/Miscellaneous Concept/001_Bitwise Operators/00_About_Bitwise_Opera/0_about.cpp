/*
1. BITWISE OPERATORS :- Bitwise operators perform operations on the binary
                        form of numbers.
        DEFINITION :- Bitwise operators in C allow manipulation of individual
                    bits of data, enabling low-level operations directly on
                    binary representations. These operators are essential for
                    tasks like hardware programming, encryption, and
                    performance optimization.

        TYPES :- There are mainly 6 types:
            1. Bitwise AND(&)
            2. Bitwise OR(|)
            3. Bitwise XOR(^)
            4. Bitwise NOT(~)
            5. Lest Shift(<<)
            6. Right Shift(>>)
*/
/*
1. LOCAL SCOPE :-
        1. We make a local variable
        2. The variables that we define inside blocks of code [{...}] like if-else,
           loops, or functions all remain within the local scope and are not
           accessible outside of it.
*/